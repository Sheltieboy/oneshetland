// Stub for @opentelemetry/api — not used in this app.
// Supabase optionally imports it for tracing; we map it here so the
// dynamic import() never reaches Hermes, which can't compile that syntax.
module.exports = null;
