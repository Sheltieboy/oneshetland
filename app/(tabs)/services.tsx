/**
 * (tabs)/services.tsx — the "Directory" tab.
 *
 * Renders the full Shetland business directory. The screen lives at
 * app/local-businesses-browse.tsx (also pushable as its own route); here it's
 * surfaced as the Directory tab. Its back button hides itself at the tab root.
 */
export { default } from '../local-businesses-browse';
