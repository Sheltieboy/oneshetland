node_modules/
.expo/
dist/
web-build/
.env
*.log
ios/
android/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Build artifacts
*.ipa
*.apk
*.aab

# macOS
.DS_Store

# Claude session files
.claude/
# local-only service role key (never commit)
service_key.txt

# local-only DB connection string for one-off pg_dump (never commit)
supabase/.db_url

# Large local-only cruise data dumps + source image pile (served from
# Supabase storage; not bundled in the app — keep out of git history)
assets/cruise-pics/
assets/cruise-dump.csv
assets/cruise-dump.sql
assets/cruise-import.csv

# Signing secrets & certificates — NEVER commit (Wallet / Apple push certs)
oneshetland-*.json
*.p12
*.b64
*.cer
*.certSigningRequest
wwdr.pem
.DS_Store
supabase/.temp/
